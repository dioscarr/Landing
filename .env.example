# Supabase configuration
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Auth0 configuration
# Get these from your Auth0 Application (Settings)
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-auth0-client-id

# GitHub settings
# Set this to the username whose repositories you want displayed on the dashboard
VITE_GITHUB_USERNAME=your-github-username
# Optional: GitHub personal access token to increase API rate limits (keep secret)
# Create a token with `repo` or `public_repo` access if needed and add it to your local
# `.env` file or repository secrets for CI as VITE_GITHUB_TOKEN
VITE_GITHUB_TOKEN=your-github-token